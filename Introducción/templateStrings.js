const nombre = 'Denís Guerrero'
const edad = 27
const pais = 'Españita'

console.log('La persona es ' + nombre + 'tiene ' + edad + 'y es de ' + pais) // Esto es simplemente introducir variables en medio de las etiquetas

console.log(`La persona es ${nombre} tiene ${edad} y es de ${pais}`) // Lo mismo pero con batics